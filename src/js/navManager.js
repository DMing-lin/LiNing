class Manager {
    constructor(data) {
        this.data = data;
        this.root = null;
    }
    init() {
        // this.renderUI();
        this.renderNavList();
        // this.eventHandler();
    }
    // renderUI() {
    //     let a = $(".nav")
    //     console.log(a);
    // }
    renderNavList() {
        let res = "";
        for (let i = 0, len = this.data.list.length; i < len; i++) {
            //每个 tab
            
        };
        // res += `

        //     `

    }
}
